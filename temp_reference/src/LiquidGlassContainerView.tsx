import type { NativeProps } from './LiquidGlassViewContainerNativeComponent';
import { View } from 'react-native';

/**
 * A view that renders multiple glass elements into a combined effect.
 */
export const LiquidGlassContainerView: React.ComponentType<NativeProps> = View;
