# Disable Directory Listing (Critical for Security)
Options -Indexes

# Protect sensitive files and directories
<FilesMatch "^\.|\.env|composer\.(json|lock)|package\.json|package-lock\.json|\.git|\.sql$">
    Order deny,allow
    Deny from all
</FilesMatch>

# Allow access to normal API endpoints
<FilesMatch "\.php$">
    Order allow,deny
    Allow from all
</FilesMatch>
